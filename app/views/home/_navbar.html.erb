<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>PesaFast</title>
</head>
<body>
    <nav class="navbar">
        <div class="navbar-logo">
            <a href="#">PesaFast</a>
        </div>
        <%=current_user.username%>
        balance <%=current_user.balance%>
        bets <%= User.count_users_with_bets%>
        Active Users: <%= User.logged_in_count %>
          
        <div class="navbar-links">
            <a class="nav-link flex-grow-1" href="<%= deposits_new_path %>" data-turbo-frame="modal">Deposit</a>
             <a class="nav-link flex-grow-1" href="<%= deposits_new_path %>" data-turbo-frame="modal">Deposit</a>
             <a class="nav-link flex-grow-1" href="<%= deposits_new_path %>" data-turbo-frame="modal">Deposit</a>
            <a class="nav-link flex-grow-1" href="<%= deposits_new_path %>" data-turbo-frame="modal">Deposit</a>
             
            <a class="dropdown-item" href="<%= withdraws_new_path %>">Withdraw</a>
            <li><hr class="dropdown-divider"></li>
            <%= button_to "Log out", logout_path, method: :delete, class: "dropdown-item" %>
          </ul>
        </li>
        </div>
    </nav>
</body>
</html>
